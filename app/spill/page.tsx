import FilesPlatform from "../components/files-platform";
import { metadataFor } from "../seo";

export const metadata = metadataFor("spill");

export default function SpillPage() {
  return <FilesPlatform page="tip" />;
}
