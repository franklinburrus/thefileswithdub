# Asset Master vs Placement Example

**Law — recorded 2026-09-21.** When Frank supplies images, confirm which file
is the asset master and which is an annotated/placement example BEFORE
rendering — a circled/marked-up image is the placement example, never the
master.

(2026-09-21: the d0… circled image was the placement example, the 4b…
file was the real badge master; REV3 also misread "use the dub logo" as the
TF monogram.)

**Canonical source — supersedes copies elsewhere.**
